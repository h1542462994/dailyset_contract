package org.tty.dailyset.contract.declare

/**
 * temporary action used by temporary link.
 * @see [org.tty.dailyset.contract.declare.ResourceTemporaryLink]
 */
enum class TemporaryAction {
    Apply,
    Remove,
}