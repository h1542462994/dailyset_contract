package org.tty.dailyset.contract.bean.enums

/**
 * used for writing resources.
 */
enum class RelatingOption {
    /**
     * resource has no relationship between sets.
     */
    Separate,

    /**
     * resource could have relationship between sets.
     */
    Relating
}