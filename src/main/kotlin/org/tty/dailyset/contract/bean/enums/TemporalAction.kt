package org.tty.dailyset.contract.bean.enums

/**
 * temporal action used by temporal link.
 * @see [org.tty.dailyset.contract.declare.ResourceTemporalLink]
 */
enum class TemporalAction {
    Apply,
    Remove,
}